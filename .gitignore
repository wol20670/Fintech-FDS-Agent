# ── Python ──
__pycache__/
*.py[cod]
*.pyo
.Python
*.egg-info/
dist/
build/
.eggs/

# ── 가상환경 ──
.venv/
venv/
env/

# ── 환경 변수 (절대 커밋 금지!) ──
.env
.env.local
*.env

# ── 데이터베이스 (자동 생성됨) ──
backend/data/*.db
backend/data/*.sqlite

# ── 학습된 모델 파일 (용량 큰 바이너리) ──
# 별도 Google Drive / DVC로 관리 권장
backend/models/*.pkl
backend/models/*.joblib

# ── 대용량 데이터 원본 파일 제외 ──
Fraud.csv
*.csv

# ── 로컬 학습 결과 리포트 이미지 제외 ──
backend/models/*.png

# ── IDE ──
.idea/
.vscode/
*.swp
*.swo

# ── OS ──
.DS_Store
Thumbs.db

# ── 로그 ──
*.log
logs/
